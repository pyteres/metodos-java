package metodos.sobrecarga;

public class sobrecarga {
    //E a capacidade de definir metodo para diferentes contextos. Mais preservando o seu nome.
    /*
     * Criaçao: //Alterar asiinatura do metodo
     * ASS = nome + parametro:
     * exemplo:
     *ConverterParaInteiro (float = f);
     ConverterParaInteiro(double = D);
     converterParaInteiro (String = S);
     converterParaInteiro (float = F, roundType = RD);
     converterParaInteiro (double = D, roundType = RD)
     converterParaInteiro (Strubg = S, roandType = RD) 
     */

     //O metodo println tabem e uma sobre carga podendo ter o mesomo nome mais diferente asinaturas.
     //valueOf tambem e uma palavra que voce pode transformar qual quer parametro em uma string.
    //Sobrecarga x sobreescrita:
    //sobrecarga: e quando se tem um metodo do mesmo nome mais assinaturas diferentes.
    //sobreEscrita: eka ve o negocio da heranaça.
}
